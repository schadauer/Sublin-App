// --------------- TODO -----------------
// This needs to repace the boolean values at some point
enum RoutingStatus {
  initial,
  active,
  unavailable,
  canceled,
  timeOut,
  booked,
  confirmed,
  completed,
}
